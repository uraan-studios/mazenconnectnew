"use client"
import React from 'react'

const OnBoardingInner = ({ createEmployee, createClass, testNotification, createPrincipalReport }: { createEmployee: boolean, createClass: boolean, testNotification: boolean, createPrincipalReport: boolean }) => {
  return (
    <div>

    </div>
  )
}

export default OnBoardingInner
